import React from "react";

const add = (a: number, b: number): number => a + b;
const App = () => {
  return <div className="container">Rsdsdeact!</div>;
};

export default App;
                         